카뱅 거래 내역 읽기, 일부 데이터(입금 확인-> 거래내역시트에 출금, 다른 이유로 거래한 부분을 걸러내고 입금한 분들의 이름만 봇이 알 수 있게 하려고 함/이름 추출), 
새 엑셀파일 생성(입금자 명단 작성)

입금확인 폼 엑셀에서 이름(학번2자리+이름)과 디스코드 번호를 추출 
새 엑셀 파일 생성(위의 두 항목 내용 포함)

엑셀 파일을 읽을 때 
엑셀파일 주소를 타고 들어가서 읽는 방식으로 했기 때문에 
(pandas에 있는 기능을 활용하려고 )

총무인 제가 실제로 사용할 땐 엑셀 시트만 다운받아주면 되서 간편해짐

여준님이 테스트 하실 땐 엑셀 주소만 여준님 컴퓨터 기준으로 바꾸면 됨
 (엑셀 우클릭 후 속성 - > 위치 부분 복사 - >  \이렇게 되어있는걸 /이걸로 바꿔야함 ->위치 맨 뒤에   /파일이름.xlsx   를 붙여줘야 함)
 
 

